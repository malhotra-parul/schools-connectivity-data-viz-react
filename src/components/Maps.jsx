import React from 'react'
import styled from "styled-components"

const Maps = () => {
    return (
        <DIV style={{  border: '1px solid red'}}>Maps</DIV>
    )
}

export default Maps
const DIV = styled.div`
    grid-row: 1/3;
    grid-column: 2;
`
